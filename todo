Check Sorts
- TIMSORT
	select run size
	while n < run_size
		reverse sort insert B
	then merge sort
vvaltone
	- insertsort Optimization?


10.1.
	
	flags for checker
	implement sa && sb? (796. 806)
	fix everything for negative numbers (and protect from outside the range of int)
		- visualiser -> if min < 1 then whole array + (min + 1)
	RUN_SIZE into struct?
	fix one number only arrays


20.01.
	Optimization:
		check if next run is sorted -> skip
		check if the run can be fixed with sa efficiently
		check if there is only one off
		insertsort -> measure distance

20.01.
	visualiser -> color up/down make it nice